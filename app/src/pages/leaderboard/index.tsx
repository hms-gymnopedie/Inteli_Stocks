/**
 * /leaderboard — placeholder shell. B8-SIM will populate this with the
 * strategy backtest leaderboard + new-strategy form.
 */
export function Leaderboard() {
  return (
    <div className="settings-page">
      <header className="settings-header">
        <h1 className="settings-title">Leaderboard</h1>
        <p className="settings-sub">Backtest results loading…</p>
      </header>
    </div>
  );
}
