/**
 * /ai-assistant — placeholder shell. B8-AI-TAB will populate this with
 * persistent per-area history (Signals / Insights / Verdicts / Hedges).
 */
export function AIAssistant() {
  return (
    <div className="settings-page">
      <header className="settings-header">
        <h1 className="settings-title">AI Assistant</h1>
        <p className="settings-sub">Per-area history loading…</p>
      </header>
    </div>
  );
}
